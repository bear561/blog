# ===========================================
# 本地开发配置 (java -jar xxx.jar --spring.profiles.active=local)
# 使用 H2 文件数据库，无需 Docker，开箱即用
# ===========================================

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./data/blog;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always
  cache:
    type: none
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

logging:
  level:
    com.blog: DEBUG

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

app:
  jwt:
    secret: local-dev-secret-not-for-production
  upload:
    path: d:/blog/uploads

# AI 配置（本地开发直接写这里）
ai:
  api-key: sk-cc680ce7b0194886bd91faf6076bd50f
  model: deepseek-v4-flash
